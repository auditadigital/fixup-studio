# engine/.env — SOLO local / server. Está gitignored (.env*). Copiá a engine/.env.
# service_role da acceso total a la DB → nunca commitear, nunca exponer al cliente.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
