DASHBOARD_USER=fixup
DASHBOARD_PASS=fixup

# Supabase — SOLO server-side (Next server / API routes). Nunca en el cliente.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# SUPABASE_ANON_KEY=   # opcional, para uso client-side futuro

# Token de ingest para el routine de research (cloud). Permite POST/GET a
# /api/prospectos con "Authorization: Bearer <token>" (bypass del Basic-auth,
# solo esa colección). Generar uno largo y aleatorio.
INGEST_TOKEN=
