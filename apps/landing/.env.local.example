# apps/landing — server env (Vercel). NUNCA exponer al cliente (sin prefijo NEXT_PUBLIC_).
# service_role da acceso total a la DB → solo en el route handler /api/leads.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Aviso de lead nuevo a la operadora (best-effort; un fallo no pierde el lead).
NOTIFY_EMAIL=
KAKAO_WEBHOOK_URL=
